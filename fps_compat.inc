  {$IFDEF FPC}
    {$IF defined(FPC_FULLVERSION) AND (FPC_FULLVERSION < 20701)}
      {$DEFINE FPCPS_COMPAT_MODE}
    {$IFEND}
    {$DEFINE SUPPORT_EXTENDED_POINTER_OPERATION}
  {$ELSE}
    {$I delphiver.inc}

    {$DEFINE NEED_PTRINT}
	  {$DEFINE NEED_TRESOLVERRESPONSE}
	  {$DEFINE NEED_TSTRINGFIELDBUFFER}
    {$DEFINE NEED_PLARGEINT}

    {$IFNDEF DELPHI2009_UP}
      {$DEFINE FPCPS_COMPAT_MODE}
    {$ENDIF}
  {$ENDIF}